import { Node } from '@lib/Node'



/*
Statement
	Given the head of a linked list, your task is to check whether the linked list is a palindrome or not. Return true if the linked list is a palindrome; otherwise, return false.

Note:
	The input linked list prior to the checking process should be identical to the list after the checking process has been completed.

	For reversing the list it could be used the helper reverse method of the Node
*/
export const isPalindrome = (head: Node | null) => {
  return false
}
